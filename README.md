
  # diogocvc - portfolio

  This is a code bundle for diogocvc - portfolio. The original project is available at https://www.figma.com/design/4tR05zTDqajrL6VvJdGjb2/diogocvc---portfolio.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  